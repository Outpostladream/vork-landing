# Genshin Impact Dialogue Skip Helper

Static download landing page for GitHub Pages. The page is English-only and the ZIP download is linked from `index.html`.

## Local preview

Open `index.html` directly in a browser or serve the folder with any static file server.
